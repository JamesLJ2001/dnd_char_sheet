version: '3.8'
services:
  db:
    image: postgres:latest
    container_name: dnd-db
    restart: always
    environment:
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: dnd_database
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: